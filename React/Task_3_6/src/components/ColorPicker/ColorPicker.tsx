import { useState } from 'react'
import type { ColorPickerProps } from './ColorPicker.types'

export const ColorPicker = ({ label, colors, selectorColor, onChange }: ColorPickerProps) => {
    return (
        <div className="flex flex-col gap-2">
            
        </div>
    )
}